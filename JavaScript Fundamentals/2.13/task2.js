"use strict"

// Какие значения выведет цикл while?

let i = 0;
while (++i < 5) alert( i ); // от 1 до 4 (возвращает текущее значение)


let y = 0;
while (y++ < 5) alert( y ); // от 1 до 5 (возвращает предыдущее значение)